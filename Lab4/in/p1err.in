start
{
    int a;
    int b;
    int c;
    int max;

    scan(3a);
    scan(b);
    scan(c);


    if (a >= b && a >= c) {
        max = a;
    } elif (b >= a && b >= c) {
        max = b;
    } else {
        max ~ c;
    }

    print(max);
}
end
